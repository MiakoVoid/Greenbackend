spring:
  datasource:
#    url: jdbc:mysql://host.docker.internal:3306/greenfinance?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    url: jdbc:mysql://localhost:3306/greenfinance?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20

logging:
  level:
    root: debug
    com.it.greenfinance: debug
    # 减少RMI相关调试日志
    sun.rmi: INFO
    javax.management: INFO

jwt:
  secret: dev-secret-key

crypto:
  secret: dev-crypto-secret

ai:
  model-enabled: true
  model-api-url: https://api-inference.modelscope.cn/v1
  model-api-key: ms-7a402fbb-d82a-418b-816f-2b08a510eecb
  model-id: Qwen/Qwen3-4B
  stream-enabled: true
  ocr-enabled: true
  advice-enabled: true
